## Generating Test Data 

1. Make sure you're in the scripts directory (i.e., suite2p/scripts). 
2. Run `python generate_test_data.py`. 
The generated test data will be in the directory data/test_outputs. Note that this will overwrite the test_outputs directory.
